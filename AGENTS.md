# flip_page Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-14

## Active Technologies
- Dart 3.11.4 / Flutter 3.41.6 stable (fvm-managed). + Flutter SDK only — `package:flutter/widgets.dart`, `package:flutter/material.dart`, `package:flutter/rendering.dart` (for `RenderRepaintBoundary.toImageSync`), `package:flutter/gestures.dart` (for the custom recognizer), `dart:ui` (for `Image`, `Canvas`, `Path`). No third-party runtime deps. (001-core-widget)
- N/A — stateless widget. Caller owns any persistence of `initialPage`. (001-core-widget)

- Dart 3.11.4 / Flutter 3.41.6 stable (fvm-managed). + Flutter SDK only (`flutter/material.dart`, `flutter/rendering.dart`, `flutter/gestures.dart`). No third-party runtime deps. (001-core-widget)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Dart 3.11.4 / Flutter 3.41.6 stable (fvm-managed).

## Code Style

Dart 3.11.4 / Flutter 3.41.6 stable (fvm-managed).: Follow standard conventions

## Recent Changes
- 001-core-widget: Added Dart 3.11.4 / Flutter 3.41.6 stable (fvm-managed). + Flutter SDK only — `package:flutter/widgets.dart`, `package:flutter/material.dart`, `package:flutter/rendering.dart` (for `RenderRepaintBoundary.toImageSync`), `package:flutter/gestures.dart` (for the custom recognizer), `dart:ui` (for `Image`, `Canvas`, `Path`). No third-party runtime deps.

- 001-core-widget: Added Dart 3.11.4 / Flutter 3.41.6 stable (fvm-managed). + Flutter SDK only (`flutter/material.dart`, `flutter/rendering.dart`, `flutter/gestures.dart`). No third-party runtime deps.

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
